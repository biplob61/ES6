const firstName = "Mariam";
const lastName = "Moni"
//const fullName = firstName + " " + lastName;
const fullName = `${firstName} ${lastName} ${50+50} ${"%"} is a good girl`;//we write many things in tamplate its ver powerfull.
console.log(fullName);

const multiline = `I Love You 
I Miss You `
console.log(multiline);
